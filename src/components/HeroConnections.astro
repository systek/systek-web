---

---

<div id="connections-canvas" class="absolute top-0 left-0 w-full h-full">
  <span class="absolute pointer-events-none text-6xl font-light">Bra</span>
  <span class="absolute pointer-events-none text-6xl font-light">folk</span>
  <span class="absolute pointer-events-none text-6xl font-light">smarte</span>
  <span class="absolute pointer-events-none text-6xl font-light">løsninger</span
  >
</div>

<script>
  import Connect from "./hero-connections.ts";
  (() => {
    const rootEl = document.getElementById("connections-canvas");
    if (!rootEl) return;

    Connect(rootEl);
  })();
</script>
