---
const { node } = Astro.props;

const { items, _key: key } = node as { items?: string[]; _key: string };
---

<div class="mt-4">
  <h3 class="text-lg font-semibold">Stikkord</h3>
  <ul class="flex flex-row gap-4 list-disc list-inside">
    {items?.map((item) => <li>{item}</li>)}
  </ul>
</div>
