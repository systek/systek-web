---
interface Props {
  node: {
    items?: string[];
  };
}

const {
  node: { items },
} = Astro.props;
---

<div class="my-grid">
  <p class="font-bold text-sm-fluid">Stikkord</p>
  <div class="flex flex-wrap gap-x-2 text-sm-fluid">
    {
      items?.map((item, index) => (
        <>
          <span>{item}</span>
          {index < items?.length - 1 && <span>·</span>}
        </>
      ))
    }
  </div>
</div>
