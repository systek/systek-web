---
import type { Image as ImageType } from "sanity";
import Image from "./Image.astro";
import Arrow from "./Arrow.astro";
import { vw } from "../utils/number";

interface Props {
  image?: ImageType;
  title?: string;
}

const { image, title } = Astro.props;
---

<div
  id="main-content"
  class="flex flex-col m-grid gap-section scroll-mt-[var(--height-header)]"
>
  <div class="flex justify-between">
    <Arrow position="topLeft" />
    <Arrow position="topRight" />
  </div>

  {
    title && (
      <h1 class="font-light text-4xl-fluid hyphens-auto md:text-center">
        {title}
      </h1>
    )
  }

  <slot />

  {
    image && (
      <>
        <div class="flex justify-between">
          <Arrow position="middleLeft" />
          <Arrow position="middleRight" />
        </div>

        <Image
          class="w-full rounded-md aspect-[4/3] md:aspect-[2/1] object-cover"
          image={image}
          loading="eager"
          mode="cover"
          width={1640}
          height={820}
          sizes={`(min-width: 105rem) calc(100vw - calc(2 * ${vw(1.25)})), (min-width: 50rem) calc(100vw - 2.5rem), calc(100vw - 2rem)`}
        />
      </>
    )
  }

  <div class="flex justify-between">
    <Arrow position="bottomLeft" />
    <Arrow position="bottomRight" />
  </div>
</div>
