---
import Button from "./Button.astro";

const links = [
  { text: "Arbeider", href: "/" },
  { text: "Tjenester", href: "/" },
  { text: "Folka", href: "/" },
];
---

<div class="w-full sticky top-0 z-100 bg-background">
  <header
    class="flex items-center justify-between py-3 px-4 border-b gap-4 border-grey-10/10"
  >
    <img src="/images/systek-logo.svg" alt="Systek" class="h-7" />
    <nav>
      <ul class="flex gap-8">
        {
          links.map((link) => (
            <li>
              <a href={link.href} class="hover:underline">
                {link.text}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <Button color="black" href="/">Jobbe her?</Button>
  </header>
</div>
