---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<div>
  {title && <p class="mb-2 font-bold text-sm-fluid lg:mb-0">{title}</p>}
</div>
<div class="lg:col-span-3">
  <slot />
</div>
