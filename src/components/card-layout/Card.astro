---
type Size = "md" | "lg";

interface Props {
  color?: "transparent" | "grey";
  class?: string;
  href: string;
  size?: Size;
}

const { color = "transparent", size = "md", class: className } = Astro.props;

const sizeClasses = {
  md: "min-h-72 lg:aspect-[4/3]",
  lg: "sm:min-h-[29rem]",
}[size];

const colorClasses = {
  transparent: "",
  grey: "bg-grey-91 hover:bg-orange",
}[color];
---

<a
  class:list={[
    "flex flex-col justify-between gap-10 p-6 rounded-lg group",
    sizeClasses,
    colorClasses,
  ]}
  href={Astro.props.href}
>
  <slot />
</a>
