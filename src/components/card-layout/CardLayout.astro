---
interface Props {
  class?: string;
  color?: "transparent" | "grey";
}

const { color = "transparent", class: className } = Astro.props;

const colorClasses = {
  transparent: "",
  grey: "",
}[color];
---

<div
  class:list={[
    "mx-grid pt-grid grid grid-cols-1 gap-gutter lg:grid-cols-4",
    colorClasses,
    className,
  ]}
>
  <slot />
</div>
